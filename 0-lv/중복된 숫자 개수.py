def solution(array, n):
    answer = array.count(n)
    # count 함수는 무언가 찾고 싶을때 사용한다.
    # 사용방법은 '변수. count(찾는 요소)' 형태로 사용한다.
    # 괄호( ) 안에 찾고자 하는 값을 입력하면 함수를 사용한 변수 안에서 해당 값의 
    # 개수를 숫자로 반환한다. 
    return answer